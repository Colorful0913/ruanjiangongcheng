<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.manage.mapper.DormStudentMapper">
    
    <resultMap type="DormStudent" id="DormStudentResult">
        <result property="studentId"    column="student_id"    />
        <result property="userId"    column="user_id"    />
        <result property="studentNumber"    column="student_number"    />
        <result property="faculty"    column="faculty"    />
        <result property="className"    column="class_name"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectDormStudentVo">
        select student_id, user_id, student_number, faculty, class_name, del_flag, create_by, create_time, update_by, update_time, remark from tb_dorm_student
    </sql>

    <select id="selectDormStudentList" parameterType="DormStudent" resultMap="DormStudentResult">
        <include refid="selectDormStudentVo"/>
        <where>  
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="studentNumber != null  and studentNumber != ''"> and student_number = #{studentNumber}</if>
            <if test="faculty != null  and faculty != ''"> and faculty = #{faculty}</if>
            <if test="className != null  and className != ''"> and class_name like concat('%', #{className}, '%')</if>
        </where>
    </select>
    
    <select id="selectDormStudentByStudentId" parameterType="Long" resultMap="DormStudentResult">
        <include refid="selectDormStudentVo"/>
        where student_id = #{studentId}
    </select>

    <insert id="insertDormStudent" parameterType="DormStudent" useGeneratedKeys="true" keyProperty="studentId">
        insert into tb_dorm_student
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="studentNumber != null and studentNumber != ''">student_number,</if>
            <if test="faculty != null">faculty,</if>
            <if test="className != null">class_name,</if>
            <if test="delFlag != null and delFlag != ''">del_flag,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="studentNumber != null and studentNumber != ''">#{studentNumber},</if>
            <if test="faculty != null">#{faculty},</if>
            <if test="className != null">#{className},</if>
            <if test="delFlag != null and delFlag != ''">#{delFlag},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateDormStudent" parameterType="DormStudent">
        update tb_dorm_student
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="studentNumber != null and studentNumber != ''">student_number = #{studentNumber},</if>
            <if test="faculty != null">faculty = #{faculty},</if>
            <if test="className != null">class_name = #{className},</if>
            <if test="delFlag != null and delFlag != ''">del_flag = #{delFlag},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where student_id = #{studentId}
    </update>

    <delete id="deleteDormStudentByStudentId" parameterType="Long">
        delete from tb_dorm_student where student_id = #{studentId}
    </delete>

    <delete id="deleteDormStudentByStudentIds" parameterType="String">
        delete from tb_dorm_student where student_id in 
        <foreach item="studentId" collection="array" open="(" separator="," close=")">
            #{studentId}
        </foreach>
    </delete>
</mapper>